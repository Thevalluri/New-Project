Public class Rectangle {
 public int length;
 public int width;

 publuc Rectangle(int length, int width) {
 this.length = length;
 this.width = width;
 }
 
 Public int getArea() {
  return length * width;
 }

 public int getPerimeter() {
  return 2 * (length + breadth);
 }
}
